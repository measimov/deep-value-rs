endpoints:
  - api_name: new_share
    family: stock_event
    domain: stock
    volume_class: S1_EVENT
    partition_template: event_year_month
    primary_date_field: issue_date
    supported_params:
      - start_date
      - end_date
    endpoint_kind: event
    planner_kind: date_backfill
    execution_status: disabled
    reason_disabled: "requires separate event date planning and field validation"
    required_infra:
      - event planner review
      - bounded date smoke
    risk_level: medium
    notes: "Inventory stub only; not loaded into CatalogStore endpoints."
